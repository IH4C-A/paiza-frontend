export type GroupMember = {
    group_member_id: string;
    user_id: string;
    group_id: string;
}

export type GroupMemberAll = {
    group_member_id: string;
    user_id: string;
    user_name: string;
    prof_image: string;
}